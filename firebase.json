{
  "_comment": [
    "Deploy config for the Firestore SECURITY RULES only — nothing else about this project",
    "touches Firebase. The app is a static page on GitHub Pages that talks to Firestore over",
    "REST with no SDK, so this file adds no dependency and no build step; it exists purely so",
    "`firebase deploy --only firestore:rules` knows which file to publish.",
    "",
    "Hosting is deliberately absent: GitHub Pages serves the site, and adding Firebase Hosting",
    "here would give the app two live origins that could drift apart.",
    "",
    "The indexes file used to be absent too, on the grounds that galList falls back to an",
    "unordered query and the gallery therefore works without one. It does — but the fallback",
    "costs a failed 400 request and a console error on every uncached gallery open, and it can",
    "only sort the arbitrary page of documents it got back. The index is now declared in",
    "firestore.indexes.json so it deploys with the rules instead of waiting for someone to",
    "notice the console link."
  ],
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  }
}
